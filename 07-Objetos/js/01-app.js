const nombre = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

//Un objeto agrupa o almencena todo en una sola variable
//Object literal

const productor = {
    nombre : "Monitor 20 pulgadas",
    precio : 300,
    disponible: true
};

console.log(productor)
console.log(typeof productor)